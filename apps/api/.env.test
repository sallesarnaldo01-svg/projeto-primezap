# =
# CONFIGURAÇÃO DE TESTE - PRIMEFLOW-HUB
# =

# DOMÍNIOS DE TESTE
VITE_APP_URL=http://localhost:3000
VITE_API_BASE_URL=http://localhost:3001
API_URL=http://localhost:3001
FRONTEND_ORIGIN=http://localhost:3000

# SUPABASE (MOCKADO PARA TESTES)
VITE_SUPABASE_URL=http://localhost:8000
VITE_SUPABASE_PUBLISHABLE_KEY=test_publishable_key
VITE_SUPABASE_PROJECT_ID=test_project
SUPABASE_URL=http://localhost:8000
SUPABASE_ANON_KEY=test_anon_key
SUPABASE_SERVICE_ROLE_KEY=test_service_role_key
VITE_WHATSAPP_PROVIDER=venom
WHATSAPP_PROVIDER=venom

# DATABASE (USANDO SQLite EM MEMÓRIA PARA TESTES)
# Nota: O projeto usa PostgreSQL, mas para testes unitários/integração leves, o SQLite em memória é comum.
# Se os testes exigirem PostgreSQL, o usuário precisará configurar um container Docker de teste.
DATABASE_URL="postgresql://testuser:testpassword@localhost:5432/testdb"
DATABASE_POOL_MIN=1
DATABASE_POOL_MAX=1

# REDIS (USANDO REDIS LOCAL PARA TESTES)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=1
REDIS_CACHE_TTL=3600
REDIS_URL=redis://localhost:6379/1

# JWT
JWT_SECRET=test_jwt_secret_for_testing_only
JWT_EXPIRES_IN=1h
JWT_REFRESH_EXPIRES_IN=7d

# WORKERS
WORKER_CONCURRENCY=1
WORKER_MAX_JOBS_PER_WORKER=10

# OPENAI/GEMINI (MOCKADO PARA TESTES)
OPENAI_API_KEY=test_openai_key
GEMINI_API_KEY=test_gemini_key

# NODE
NODE_ENV=test
